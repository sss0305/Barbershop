<h2>Bookings</h2>

<table border="1">
  <tr>
  	<th>ID</th>
    <th>Имя</th>
    <th>Телефон</th>
    <th>Дата</th>
    <th>Барбер</th>
    <th>Цвет</th>
  </tr>

<% @results.each do |row| %>

  <tr>
  	<td><%= row['id'] %></td>
    <td><%= row['username'] %></td>
    <td><%= row['phone'] %></td>
    <td><%= row['datestamp'] %></td>
    <td><%= row['barber'] %></td>
    <td><%= row['color'] %></td>
  </tr>

<% end %>

</table>